import Heading from "@/components/heading";
import Navbar from "./navbar";

const Header = () => {
  return (
    <header>
      <Navbar/>
      {/* <Heading tag={"h3"} text={"Some header text"} /> */}
    </header>
  );
};

export default Header;
